import AuthForm from "@/components/AuthForm";
import React from "react";

const page = () => {
  /* Masukkan type ke parameter AuthForm sebagai props */
  return <AuthForm type="sign-in" />;
};

export default page;
